services:
  # ==========================================
  # 1. KEYCLOAK SERVER
  # ==========================================
  keycloak:
    image: quay.io/keycloak/keycloak:24.0.0
    container_name: food-delivery-keycloak
    command: start-dev --import-realm
    env_file:
      - .env.infra # 🚀 Ép container đọc file .env.infra tại gốc
    environment:
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN_USER}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
      KC_HTTP_PORT: ${KEYCLOAK_PORT}
      KC_THEME_CACHE_THEMES: "false"
      KC_THEME_CACHE_TEMPLATES: "false"
      KC_HOSTNAME_URL: ${KEYCLOAK_EXTERNAL_URL}
      KC_HOSTNAME_STRICT: "false"
      KC_HOSTNAME_STRICT_BACKCHANNEL: "false"
    ports:
      - "${KEYCLOAK_PORT}:${KEYCLOAK_PORT}"
    volumes:
      - keycloak_data:/opt/keycloak/data
      - ${KEYCLOAK_THEME_PATH}/keycloak-theme-for-kc-22-to-25.jar:/opt/keycloak/providers/my-theme.jar
      - ./development-template/keycloak-realm/realm-export.json:/opt/keycloak/data/import/realm-export.json
    networks:
      - food-delivery-network

  # ==========================================
  # 2. KAFKA BROKER (Chuẩn KRaft)
  # ==========================================
  kafka:
    image: confluentinc/cp-kafka:7.4.0
    container_name: food-delivery-kafka
    env_file:
      - .env.infra # 🚀 Ép container đọc file .env.infra tại gốc
    ports:
      - "${KAFKA_PORT_EXTERNAL}:${KAFKA_PORT_EXTERNAL}"
    environment:
      KAFKA_NODE_ID: ${KAFKA_NODE_ID}
      KAFKA_PROCESS_ROLES: 'broker,controller'
      KAFKA_CONTROLLER_QUORUM_VOTERS: '${KAFKA_NODE_ID}@kafka:${KAFKA_PORT_CONTROLLER}'
      KAFKA_LISTENERS: 'EXTERNAL://0.0.0.0:${KAFKA_PORT_EXTERNAL},INTERNAL://0.0.0.0:${KAFKA_PORT_INTERNAL},CONTROLLER://0.0.0.0:${KAFKA_PORT_CONTROLLER}'
      KAFKA_ADVERTISED_LISTENERS: 'EXTERNAL://${KAFKA_HOSTNAME_EXTERNAL}:${KAFKA_PORT_EXTERNAL},INTERNAL://kafka:${KAFKA_PORT_INTERNAL}'
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: 'CONTROLLER:PLAINTEXT,EXTERNAL:PLAINTEXT,INTERNAL:PLAINTEXT'
      KAFKA_CONTROLLER_LISTENER_NAMES: 'CONTROLLER'
      KAFKA_INTER_BROKER_LISTENER_NAME: 'INTERNAL'
      
      CLUSTER_ID: ${KAFKA_CLUSTER_ID}
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
    networks:
      - food-delivery-network
  
  # ==========================================
  # 3. CONFIG PROCESSOR (Đã sửa lại đường dẫn /config)
  # ==========================================
  config-processor:
    image: alpine:3.18
    container_name: food-delivery-config-processor
    env_file:
      - .env.infra
    volumes:
      - ./:/config
    environment:
      - IDENTITY_SERVICE_PORT=${IDENTITY_SERVICE_PORT}
      - API_GATEWAY_PORT=${API_GATEWAY_PORT}
      - LOKI_PORT=${LOKI_PORT}
      - PROMTAIL_PORT=${PROMTAIL_PORT}
      - PROMETHEUS_PORT=${PROMETHEUS_PORT}
      - GRAFANA_PORT=${GRAFANA_PORT}
    entrypoint:
      - "sh"
      - "-c"
      - |
        apk add --no-cache gettext > /dev/null

        # Sửa: Đưa /config vào trước tất cả các đường dẫn để lưu về máy thật
        mkdir -p /config/development-template/yml-generated/prometheus
        envsubst < /config/development-template/yml-template/prometheus/prometheus.yml.template > /config/development-template/yml-generated/prometheus/prometheus.yml

        mkdir -p /config/development-template/yml-generated/promtail
        envsubst < /config/development-template/yml-template/promtail/config.yml.template > /config/development-template/yml-generated/promtail/config.yml

        mkdir -p /config/development-template/yml-generated/loki
        envsubst < /config/development-template/yml-template/loki/local-config.yml.template > /config/development-template/yml-generated/loki/local-config.yaml

        mkdir -p /config/development-template/yml-generated/grafana/datasources
        envsubst < /config/development-template/yml-template/grafana/datasources.yml.template > /config/development-template/yml-generated/grafana/datasources/datasources.yaml

        mkdir -p /config/development-template/yml-generated/grafana/dashboards/json
        envsubst < /config/development-template/yml-template/grafana/dashboards.yml.template > /config/development-template/yml-generated/grafana/dashboards/dashboards.yaml

        # Sửa đường dẫn file json nguồn
        cp /config/development-template/grafana-dashboards/services-monitor-dashboard.json /config/development-template/yml-generated/grafana/dashboards/json/services-monitor-dashboard.json

        echo "SUCCESS: Đã tạo cây cấu hình chuẩn xác vào thư mục development-template!"
    networks:
      - food-delivery-network

  # ==========================================
  # 4. PROMETHEUS SERVER
  # ==========================================
  prometheus:
    image: prom/prometheus:v2.45.0
    container_name: food-delivery-prometheus
    env_file:
      - .env.infra
    ports:
      - "${PROMETHEUS_PORT}:${PROMETHEUS_PORT}"
    volumes:
      # Sửa: Trỏ đúng vào thư mục con development-template
      - ./development-template/yml-generated/prometheus:/etc/prometheus
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      config-processor:
        condition: service_completed_successfully
    networks:
      - food-delivery-network

  # ==========================================
  # 5. GRAFANA
  # ==========================================
  grafana:
    image: grafana/grafana:10.0.0
    container_name: food-delivery-grafana
    env_file:
      - .env.infra
    ports:
      - "${GRAFANA_PORT}:${GRAFANA_PORT}"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}
    volumes:
      # Sửa: Trỏ đúng vào thư mục con development-template
      - ./development-template/yml-generated/grafana:/etc/grafana/provisioning
    depends_on:
      config-processor:
        condition: service_completed_successfully
      prometheus:
        condition: service_started
      loki:
        condition: service_started
    networks:
      - food-delivery-network

  # ==========================================
  # 6. LOKI
  # ==========================================
  loki:
    image: grafana/loki:2.8.2
    container_name: food-delivery-loki
    env_file:
      - .env.infra
    ports:
      - "${LOKI_PORT}:${LOKI_PORT}"
    volumes:
      # Sửa: Trỏ đúng vào thư mục con development-template
      - ./development-template/yml-generated/loki:/etc/loki
    environment:
      - LOKI_PORT=${LOKI_PORT}
    depends_on:
      config-processor:
        condition: service_completed_successfully
    networks:
      - food-delivery-network

  # ==========================================
  # 7. PROMTAIL
  # ==========================================
  promtail:
    image: grafana/promtail:2.8.2
    container_name: food-delivery-promtail
    env_file:
      - .env.infra
    volumes:
      # Sửa: Trỏ đúng vào thư mục con development-template
      - ./development-template/yml-generated/promtail:/etc/promtail
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      config-processor:
        condition: service_completed_successfully
      loki:
        condition: service_started
    networks:
      - food-delivery-network

volumes:
  keycloak_data:

networks:
  food-delivery-network:
    name: food-delivery-network
    driver: bridge